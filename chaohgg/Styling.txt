/* ==================================================
   Module Map
   - Rendering fundamentals (tokens, layout, typography)
   - Deck & Tag presentation
   - Cloze / Blank interactions
   - Reveal / Expand / Popup interactions
   - Choice questions
   - Toggle utilities (hide/reveal, divider, etc.)
   - Image occlusion (reserved)
   - Audio player scaffolding (reserved)
   ================================================== */

/* -------------------------------------------------- 渲染 · 参数 */
:root {
    --card-max-width: 46em;
    --card-text-align: left;
    --font-primary: "Georgia", "Yasong", Arial, serif;
    --font-secondary: "Sassoon Book", "Youkai", Arial, serif;
    --font-blank: "Sassoon Montessori", "Jiashu", Arial, serif;
    --font-size-base: 16px;
    --font-size-lg: 18px;
    --font-size-xl: 20px;
    --img-width: 50%;
    --img-brightness: 1;
    --img-filter: none;
}

html {
    font-size: var(--font-size-lg);
}

/* -------------------------------------------------- 渲染 · 色彩 */
.card {
    background-color: #e4dfd7;
    --card-bg: #fffefa;
    --card-border: transparent;
    --card-shadow: #d4c4b740;
    --text-primary: #475164;
    --text-fg-selected: #fff;
    --text-bg-selected: #5e7987;
    --blank-fg: #126e82;
    --popup-bg: #db8540;
    --comment-bg: #363433;
    --comment-fg: #fffef9;
    --reveal-fg: #2376b7;
    --reveal-fg-hover: #134857;
    --reveal-bg-hover: #f7c173;
    --caption-fg: #826b48;
    --caption-border: #e5d3aa;
    --caption-fg-hover: #4d4030;
    --caption-fg-active: #fffef9;
    --caption-bg-active: #87723e;
    --option-bg: #f7f4ed;
    --option-border: transparent;
    --feedback-fg: var(--card-bg);
    --feedback-bg: #c1b2a3;
    --option-fg-hover: #624941;
    --option-bg-hover: #dad4cb;
    --correct-fg: #1a6840;
    --correct-bg: #f0f5e5;
    --incorrect-fg: #a61b29;
    --incorrect-bg: #fbeee2;
    --incomplete-fg: #5e5314;
    --incomplete-bg: #f9f4dc;
    --bold-fg: #a7535a;
    --italic-fg: #b14b28;
    --bold-italic-fg: #cf4813;
    --underline-fg: #497568;
    --divider: #8a988e;
    --table-fg: var(--caption-fg);
    --table-bg: var(--panel-bg);
    --table-border: rgba(229, 211, 170, 0.72);
    --table-header-bg: rgba(229, 211, 170, 0.42);
    --table-header-fg: var(--caption-fg);
    --table-row-odd-bg: rgba(229, 211, 170, 0.16);
    --table-row-even-bg: rgba(229, 211, 170, 0.28);
    --table-shadow: rgba(107, 83, 53, 0.14);
    --link-fg: #2775b6;
    --link-fg-active: #1177b0;
    --deck-fg: var(--caption-fg);
    --tag-fg: #333333cc;
    --tag-bg: #3333330f;
    --tag-fg-active: #333333;
    --tag-bg-active: #3333331a;
}

.card.nightMode {
    background-color: #22202e;
    --card-bg: #322f3b;
    --card-border: transparent;
    --card-shadow: #13112440;
    --text-primary: #c0c4c3;
    --text-fg-selected: #000;
    --text-bg-selected: #a7a8bd;
    --blank-fg: #66a9c9;
    --popup-bg: #db8540;
    --comment-bg: #373834;
    --comment-fg: #e2e1e4;
    --reveal-fg: #edc3ae;
    --reveal-fg-hover: #f8e0b0;
    --reveal-bg-hover: #525288;
    --caption-fg: #869d9d;
    --caption-border: #b2bbbe;
    --caption-fg-hover: #474b4c;
    --caption-fg-active: #35333c;
    --caption-bg-active: #93b5cf;
    --option-bg: #3e3841;
    --option-border: transparent;
    --feedback-fg: #c4cbcf;
    --feedback-bg: #47484c;
    --option-fg-hover: #fffef8;
    --option-bg-hover: #5d655f;
    --correct-fg: #b2cf87;
    --correct-bg: transparent;
    --incorrect-fg: #e77c8e;
    --incorrect-bg: transparent;
    --incomplete-fg: #e2c17c;
    --incomplete-bg: transparent;
    --bold-fg: #f07c82;
    --italic-fg: #ea8958;
    --bold-italic-fg: #f86b1d;
    --underline-fg: #69a794;
    --divider: #74759b;
    --link-fg: #5cb3cc;
    --link-fg-active: #29b7cb;
    --tag-fg: #ffffffb3;
    --tag-bg: #ffffff14;
    --tag-fg-active: #ffffff;
    --tag-bg-active: #ffffff1f;
}


/* -------------------------------------------------- 渲染 · 背景 */
.card {
    cursor: default;
    padding: 0.5em 0;
}

html:not(.mobile) .card {
    padding: 0.5em;
}

.card::-webkit-scrollbar {
  display: none;
}

/* -------------------------------------------------- 渲染 · 卡片布局 */
.chaohgg-flashcard {
    background-color: transparent;
    border: none;
    box-shadow: none;
    color: var(--text-primary);
    font-family: var(--font-primary);
    font-size: 1rem;
    line-height: 1.7;
    margin: 0 auto;
    max-width: var(--card-max-width);
    text-align: var(--card-text-align);
    word-wrap: break-word;
}

.chaohgg-flashcard ::selection {
    background-color: var(--text-bg-selected);
    color: var(--text-fg-selected);
}

/* -------------------------------------------------- 渲染 · 字段 */

/* -------------------------------------------------- 渲染 · 适配器扩展 */
.chaohgg-mermaid {
    display: flex;
    justify-content: center;
    margin: 1.5em auto;
    max-width: 100%;
    text-align: center;
    overflow-x: auto;
    overflow-y: hidden;
    scrollbar-width: none;
}

.chaohgg-mermaid svg {
    display: block;
    max-width: 100%;
    height: auto;
    flex: 0 0 auto;
}

.chaohgg-mermaid:hover {
    scrollbar-width: thin;
}

.chaohgg-mermaid::-webkit-scrollbar {
    height: 0;
}

.chaohgg-mermaid:hover::-webkit-scrollbar {
    height: 6px;
}

.chaohgg-mermaid:hover::-webkit-scrollbar-thumb {
    background: rgba(0, 0, 0, 0.35);
    border-radius: 3px;
}

#chaohgg-comment-overlay {
    pointer-events: none;
    font: inherit;
    color: inherit;
}

#chaohgg-comment-overlay .comment {
    pointer-events: auto;
}

.chaohgg-render-error {
    margin: 1em 0;
    padding: 0.75em 1em;
    border-radius: 0.4em;
    background-color: rgba(166, 27, 41, 0.08);
    color: var(--incorrect-fg);
    font-size: 0.6em;
}

.card.nightMode .chaohgg-render-error {
    background-color: rgba(231, 124, 142, 0.12);
    color: var(--incorrect-fg);
}


/* -------------------------------------------------- 渲染 · 代码块 */
.chaohgg-field pre {
    max-width: 100%;
    overflow-x: auto;
    padding: 0.75em 1em;
    margin: 1em 0;
    background-color: rgba(247, 244, 237, 0.8);
    border-radius: 0.5em;
    border: 1px solid var(--card-border);
    scrollbar-width: none;
}

.chaohgg-field pre::-webkit-scrollbar {
    height: 0;
}

.chaohgg-field pre:hover {
    scrollbar-width: thin;
}

.chaohgg-field pre:hover::-webkit-scrollbar {
    height: 6px;
}

.chaohgg-field pre:hover::-webkit-scrollbar-thumb {
    background: rgba(0, 0, 0, 0.35);
    border-radius: 3px;
}

.chaohgg-field pre code {
    display: block;
    white-space: pre;
    word-break: normal;
}

.card.nightMode .chaohgg-field pre {
    background-color: rgba(62, 56, 65, 0.6);
    border-color: rgba(255, 255, 255, 0.08);
}

.chaohgg-field .katex-display {
    max-width: 100%;
    overflow-x: auto;
    overflow-y: hidden;
    margin: 1.2em 0;
    padding: 0.5em 0.25em;
    text-align: center;
    scrollbar-width: none;
}

.chaohgg-field .katex-display:hover {
    scrollbar-width: thin;
}

.chaohgg-field .katex-display::-webkit-scrollbar {
    height: 0;
}

.chaohgg-field .katex-display:hover::-webkit-scrollbar {
    height: 6px;
}

.card.nightMode .chaohgg-field .katex-display {
    background-color: transparent;
}



/* -------------------------------------------------- 渲染 · KaTeX 核心布局 */


/* -------------------------------------------------- 模块 · 填空 */
.blank {
    cursor: pointer;
    color: var(--blank-fg);
    padding: 0 0.4em 0.06em;
    margin: 0 0.2em;
    border-bottom: 2px solid;
    font-family: "Sassoon Montessori", "Jiashu";
    text-align: center;
    font-weight: bold;
    letter-spacing: 0.06em;
    display: inline-block;
}

.blank-wrapper {
    display: inline-flex;
    align-items: center;
    gap: 0.3em;
}

.blank-hint-trigger {
    display: inline-flex;
    align-items: center;
}

.blank-revealed {
    animation: chaohgg-blank-reveal 0.35s ease-out;
}

@keyframes chaohgg-blank-reveal {
    0% {
        transform: scale(0.9);
        background-color: rgba(255, 255, 255, 0.0);
    }
    60% {
        transform: scale(1.05);
        background-color: rgba(255, 255, 255, 0.3);
    }
    100% {
        transform: scale(1);
        background-color: rgba(255, 255, 255, 0.0);
    }
}

/* -------------------------------------------------- 音频播放器 */
.chaohgg-setting-group {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75em;
    margin-bottom: 0.9em;
    font-size: 0.75rem;
}

.chaohgg-setting-label {
    color: var(--deck-fg);
}

.chaohgg-setting-hint {
    font-size: 0.68rem;
    color: var(--deck-fg);
    opacity: 0.82;
}

.chaohgg-switch {
    display: inline-flex;
    align-items: center;
    gap: 0.5em;
    cursor: pointer;
    color: var(--deck-fg);
    font-size: 0.78rem;
}

.chaohgg-switch input {
    display: none;
}

.chaohgg-switch-track {
    position: relative;
    width: 2.6em;
    height: 1.4em;
    border-radius: 999px;
    background: var(--option-bg);
    border: 1px solid var(--card-border);
    transition: background 0.2s ease, border 0.2s ease;
}

.chaohgg-switch-thumb {
    position: absolute;
    top: 0.1em;
    left: 0.1em;
    width: 1.1em;
    height: 1.1em;
    border-radius: 50%;
    background: var(--card-bg);
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.12);
    transition: transform 0.2s ease;
}

.chaohgg-switch input:checked + .chaohgg-switch-track {
    background: var(--link-fg);
    border-color: var(--link-fg);
}

.chaohgg-switch input:checked + .chaohgg-switch-track .chaohgg-switch-thumb {
    transform: translateX(1.2em);
}

.card.nightMode .chaohgg-switch-track {
    background: rgba(255, 255, 255, 0.12);
    border-color: transparent;
}

.card.nightMode .chaohgg-switch input:checked + .chaohgg-switch-track {
    background: var(--link-fg);
}

.chaohgg-player-root {
    position: fixed;
    inset: 0;
    z-index: 60;
    display: none !important;
    pointer-events: none;
    font-family: var(--font-secondary);
    --chaohgg-player-control-size: clamp(1.0rem, 5vw, 1.5rem);
    --chaohgg-player-control-width: calc(var(--chaohgg-player-control-size) * 1.9);
    --chaohgg-player-control-font: calc(var(--chaohgg-player-control-size) * 0.34);
    --chaohgg-player-gap: calc(var(--chaohgg-player-control-size) * 0.16);
    --chaohgg-player-progress-height: calc(var(--chaohgg-player-control-size) * 0.12);
    --chaohgg-player-thumb-size: calc(var(--chaohgg-player-control-size) * 0.3);
    --chaohgg-player-tooltip-offset: calc(var(--chaohgg-player-control-size) * 0.68);
    --chaohgg-player-tooltip-font: calc(var(--chaohgg-player-control-size) * 0.28);
    --chaohgg-player-tooltip-padding-y: calc(var(--chaohgg-player-control-size) * 0.08);
    --chaohgg-player-tooltip-padding-x: calc(var(--chaohgg-player-control-size) * 0.18);
}

.chaohgg-player-root * {
    box-sizing: border-box;
}

.chaohgg-player-docked-button {
    position: fixed;
    right: clamp(16px, 5vw, 32px);
    bottom: clamp(18px, 6vh, 36px);
    width: 3.4rem;
    height: 3.4rem;
    border: none;
    border-radius: 999px;
    background: var(--popup-bg);
    color: var(--text-fg-selected);
    box-shadow: 0 14px 26px rgba(0, 0, 0, 0.22);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    pointer-events: auto;
    cursor: pointer;
    transition: transform 0.32s ease, opacity 0.32s ease;
    isolation: isolate;
}

.chaohgg-player-docked-icon::before {
    content: '\266B';
    font-size: 1.2rem;
    line-height: 1;
}

.chaohgg-player-docked-progress {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: conic-gradient(var(--popup-bg) calc(var(--chaohgg-player-progress, 0) * 360deg), rgba(255, 255, 255, 0.12) 0deg);
    opacity: 0.35;
    pointer-events: none;
}

.chaohgg-player-root[data-player-view="panel"] .chaohgg-player-docked-button,
.chaohgg-player-root[data-player-view="bar"] .chaohgg-player-docked-button {
    opacity: 0;
    transform: scale(0.8);
    pointer-events: none;
}

.chaohgg-player-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(71, 81, 100, 0.55);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
}

.card.nightMode .chaohgg-player-backdrop,
.nightMode .chaohgg-player-backdrop {
    background: rgba(0, 0, 0, 0.65);
}

.chaohgg-player-root[data-player-view="panel"] .chaohgg-player-backdrop {
    opacity: 1;
    pointer-events: auto;
}

.chaohgg-player-panel {
    position: fixed;
    left: 0;
    right: 0;
    bottom: 0;
    background: var(--card-bg);
    border-radius: 20px 20px 0 0;
    box-shadow: 0 -24px 48px rgba(0, 0, 0, 0.25);
    transform: translateY(100%);
    opacity: 0;
    pointer-events: none;
    transition: transform 0.35s ease, opacity 0.35s ease;
    max-height: min(540px, 70vh);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem 1.25rem 1.5rem;
}

.chaohgg-player-root[data-player-view="panel"] .chaohgg-player-panel {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
}

.chaohgg-player-panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
}

.chaohgg-player-panel-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
}

.chaohgg-player-panel-close {
    background: var(--option-bg);
    border: none;
    color: var(--text-primary);
    border-radius: 999px;
    width: 2rem;
    height: 2rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
}

.chaohgg-player-panel-close::before,
.chaohgg-player-panel-close::after {
    content: '';
    position: absolute;
    width: 1rem;
    height: 2px;
    background: currentColor;
    border-radius: 99px;
}

.chaohgg-player-panel-close::before {
    transform: rotate(45deg);
}

.chaohgg-player-panel-close::after {
    transform: rotate(-45deg);
}

.chaohgg-player-panel-body {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    overflow-y: auto;
}

.chaohgg-player-panel-body .chaohgg-setting-group {
    margin-bottom: 0;
    align-items: center;
}

.chaohgg-player-sources-group {
    flex-direction: column;
    align-items: flex-start;
}

.chaohgg-player-panel-body select {
    width: 100%;
    padding: 0.45rem 0.6rem;
    border-radius: 0.5rem;
    border: 1px solid var(--option-border);
    background: var(--option-bg);
    color: var(--text-primary);
}

.chaohgg-player-bar {
    position: fixed;
    left: 50%;
    bottom: clamp(14px, 5vh, 26px);
    transform: translate(-50%, calc(100% + 18px));
    background: var(--card-bg);
    border-radius: 16px;
    border: 1px solid var(--card-border);
    box-shadow: 0 14px 24px rgba(0, 0, 0, 0.16);
    width: min(440px, 88vw);
    padding: calc(var(--chaohgg-player-control-size) * 0.14) calc(var(--chaohgg-player-control-size) * 0.18) calc(var(--chaohgg-player-control-size) * 0.14);
    opacity: 0;
    pointer-events: none;
    transition: transform 0.3s ease, opacity 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: calc(var(--chaohgg-player-gap) * 0.5);
}

.card.nightMode .chaohgg-player-bar,
.nightMode .chaohgg-player-bar {
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.38);
    background: rgba(39, 42, 54, 0.96);
}

.chaohgg-player-root[data-player-view="bar"] .chaohgg-player-bar {
    transform: translate(-50%, 0);
    opacity: 1;
    pointer-events: auto;
}

.chaohgg-player-bar-row {
    display: flex;
    align-items: center;
    gap: calc(var(--chaohgg-player-gap) * 0.7);
    width: 100%;
}

.chaohgg-player-bar-row--top {
    justify-content: space-between;
}

.chaohgg-player-speed-wrap {
    position: relative;
    width: var(--chaohgg-player-control-width);
    flex-shrink: 0;
    display: inline-flex;
    justify-content: center;
}

.chaohgg-player-button {
    width: var(--chaohgg-player-control-width);
    min-width: var(--chaohgg-player-control-width);
    height: var(--chaohgg-player-control-size);
    border-radius: calc(var(--chaohgg-player-control-size) * 0.46);
    border: none;
    background: var(--option-bg);
    color: var(--deck-fg);
    font-size: var(--chaohgg-player-control-font);
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s ease, color 0.2s ease;
}

.chaohgg-player-button:disabled {
    opacity: 0.4;
    cursor: not-allowed;
}

.chaohgg-player-button:not(:disabled):hover,
.chaohgg-player-button:not(:disabled):focus-visible {
    background: var(--option-bg-hover);
    outline: none;
}

.chaohgg-player-loop.is-active {
    background: var(--link-fg);
    color: var(--text-fg-selected);
}

.chaohgg-player-progress-wrap {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.4rem;
    align-items: center;
    width: 100%;
}

.chaohgg-player-time {
    font-size: 0.66rem;
    color: var(--deck-fg);
    min-width: 3rem;
    text-align: center;
}

.chaohgg-player-progress-track {
    position: relative;
    width: 100%;
}

.chaohgg-player-progress {
    appearance: none;
    width: 100%;
    height: var(--chaohgg-player-progress-height);
    background: var(--option-bg);
    border-radius: 999px;
    outline: none;
    cursor: pointer;
}

.chaohgg-player-progress::-webkit-slider-runnable-track,
.chaohgg-player-progress::-moz-range-track {
    height: 100%;
    background: var(--option-bg);
    border-radius: 999px;
}

.chaohgg-player-progress::-webkit-slider-thumb,
.chaohgg-player-progress::-moz-range-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: var(--chaohgg-player-thumb-size);
    height: var(--chaohgg-player-thumb-size);
    border-radius: 50%;
    background: var(--link-fg);
    border: 2px solid var(--card-bg);
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.18);
}

.chaohgg-player-progress.is-scrubbing {
    height: 0.5rem;
}

.chaohgg-player-tooltip {
    position: absolute;
    top: -1.8rem;
    left: calc(var(--chaohgg-tooltip-position, 0) * 1%);
    transform: translateX(-50%);
    background: var(--popup-bg);
    color: var(--card-bg);
    padding: 0.2rem 0.5rem;
    border-radius: 0.45rem;
    font-size: 0.68rem;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
}

.chaohgg-player-tooltip.is-visible {
    opacity: 1;
}

.chaohgg-player-control-buttons {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    justify-content: flex-end;
}

.chaohgg-player-settings-btn::before {
    content: '\2699';
    font-size: 1rem;
}

.chaohgg-player-message {
    font-size: 0.72rem;
    margin-top: 0.5rem;
    text-align: center;
    min-height: 1.1rem;
    color: var(--deck-fg);
}

.chaohgg-player-message.is-error {
    color: var(--incorrect-fg);
}

.chaohgg-player-message.is-info {
    color: var(--deck-fg);
}

.chaohgg-player-speed-menu {
    position: absolute;
    bottom: calc(100% + 0.45rem);
    left: 50%;
    background: var(--option-bg);
    border-radius: 0.65rem;
    box-shadow: 0 16px 26px rgba(0, 0, 0, 0.16);
    padding: 0.4rem;
    display: grid;
    gap: 0.35rem;
    opacity: 0;
    pointer-events: none;
    transform: translate(-50%, 8px);
    transition: opacity 0.2s ease, transform 0.2s ease;
    width: calc(var(--chaohgg-player-control-size) * 0.92);
    min-width: calc(var(--chaohgg-player-control-size) * 0.92);
    z-index: 2;
    transform-origin: top center;
}

.chaohgg-player-speed-menu.is-open {
    opacity: 1;
    pointer-events: auto;
    transform: translate(-50%, 0);
}

.chaohgg-player-speed-option {
    background: none;
    border: none;
    border-radius: 0.5rem;
    padding: 0.4rem 0.55rem;
    text-align: center;
    cursor: pointer;
    color: var(--text-primary);
    font-size: 0.75rem;
}

.chaohgg-player-speed-option.is-active,
.chaohgg-player-speed-option:hover,
.chaohgg-player-speed-option:focus-visible {
    background: var(--link-fg);
    color: var(--text-fg-selected);
}

.chaohgg-player-source-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
}

.chaohgg-player-source-item {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.25rem 0.52rem;
    background: var(--option-bg);
    border-radius: 0.56rem;
    color: var(--deck-fg);
    font-size: 0.72rem;
    cursor: pointer;
    user-select: none;
}

.chaohgg-player-source-title {
    font-weight: 600;
}

.chaohgg-player-source-count {
    font-variant-numeric: tabular-nums;
    opacity: 0.7;
}

.chaohgg-player-source-item:focus {
    outline: 2px solid var(--reveal-fg);
    outline-offset: 2px;
}

.chaohgg-player-source-item.is-active {
    background: var(--reveal-bg-hover);
    color: var(--reveal-fg);
}

.card.nightMode .chaohgg-player-source-item.is-active,
.nightMode .chaohgg-player-source-item.is-active {
    background: var(--caption-bg-active);
    color: var(--caption-fg-active);
}

.card.nightMode .chaohgg-player-root,
.nightMode .chaohgg-player-root {
    color: var(--text-primary);
}

.card.nightMode .chaohgg-player-panel,
.nightMode .chaohgg-player-panel {
    background: rgba(39, 42, 54, 0.98);
}

.card.nightMode .chaohgg-player-button,
.nightMode .chaohgg-player-button {
    background: rgba(255, 255, 255, 0.12);
    color: var(--text-primary);
}

.card.nightMode .chaohgg-player-speed-option,
.nightMode .chaohgg-player-speed-option {
    color: var(--text-primary);
}

.card.nightMode .chaohgg-player-source-item,
.nightMode .chaohgg-player-source-item {
    background: rgba(255, 255, 255, 0.12);
    color: var(--text-primary);
}

@media (max-width: 540px) {
    .chaohgg-player-bar {
        width: min(90vw, 380px);
        padding: calc(var(--chaohgg-player-control-size) * 0.12);
    }
    .chaohgg-player-bar-row--bottom {
        flex-direction: column;
        align-items: stretch;
        gap: calc(var(--chaohgg-player-gap) * 0.5);
    }
    .chaohgg-player-control-buttons {
        justify-content: flex-end;
    }
    .chaohgg-player-panel {
        padding: 0.85rem 0.9rem 1.25rem;
    }
    .chaohgg-player-panel-body .chaohgg-setting-group {
        flex-direction: column;
        align-items: flex-start;
    }
}

.blank-revealed {
    animation: chaohgg-blank-reveal 0.35s ease-out;
}

.key {
    opacity: 0;
    transition: all 0.3s ease-in-out;
}

.entry {
    display: none;
    transition: all 0.3s ease-in-out;
}

.chaohgg-title-audio {
    display: none !important;
}

.chaohgg-example-audio {
    display: none !important;
}

/* -------------------------------------------------- 模块 · 批注 / Popup */
.popup {
    display: inline-block;
    cursor: pointer;
    line-height: 100%;
    font-weight: bold;
    text-align: center;
    padding: 0.2em 0.2em;
    margin: 0 0.2em;
    background-color: var(--popup-bg);
    color: var(--card-bg);
    border-radius: 0.7em;
    font-size: 64%;
    user-select: none;
    vertical-align: middle;
}

.comment {
    line-height: normal;
    font-weight: normal;
    font-size: 0.8em;
    opacity: 0;
    visibility: hidden;
    background-color: var(--comment-bg);
    color: var(--comment-fg);
    text-align: justify;
    border-radius: 0.5em;
    padding: 0.5em;
    position: fixed;
    z-index: 1;
    transition: all 0.3s ease-in-out;
    max-width: 75%;
    white-space: nowrap;
    box-shadow: 0 0 8px rgba(0, 0, 0, 0.6);
}

.comment::after {
    content: "";
    position: absolute;
    top: var(--comment-after-top, auto);
    bottom: var(--comment-after-bottom, -8px);
    left: 50%;
    transform: translateX(-50%);
    margin-left: var(--comment-after-left, 0px);
    border: 8px solid transparent;
    border-top: var(--comment-after-border-top, 8px solid var(--comment-bg));
    border-bottom: var(--comment-after-border-bottom, none);
}


/* -------------------------------------------------- 模块 · 选择题 */
.option {
    display: flex;
    align-items: baseline;
    font-size: 1em;
    margin: 0.2em 0;
    padding: 0.2em;
    border: 2px solid var(--option-border);
    border-radius: 0.25em;
    background-color: var(--option-bg);
    transition: all 0.3s ease-in-out;
}

.option-header {
    font-size: 1em;
    width: 1.5em;
    position: relative;
    text-align: center;
    margin: 0;
    flex-shrink: 0;
}

.option-desc {
    overflow: hidden;
    text-overflow: ellipsis;
    flex-grow: 1;
}

.option-seq {
    display: none;
}

.feedback {
    font-size: 0.8em;
    margin: 0.2em 0;
    padding: 0.2em;
    background-color: var(--feedback-bg);
    color: var(--feedback-fg);
    display: block;
    font-weight: bold;
    cursor: pointer;
    border-radius: 0.25em;
    transition: all 0.3s ease-in-out;
    text-align: center;
    user-select: none;
}

.feedback:hover,
.option:has(input:not(:disabled)):hover {
    cursor: pointer;
    background-color: var(--option-bg-hover);
    color: var(--option-fg-hover);
}

.correct {
    background-color: var(--correct-bg);
    color: var(--correct-fg);
}

.incorrect {
    background-color: var(--incorrect-bg);
    color: var(--incorrect-fg);
}

.incomplete,
.not-selected {
    background-color: var(--incomplete-bg);
    color: var(--incomplete-fg);
}

.nightMode .correct,
.nightMode .incorrect,
.nightMode .not-selected {
    border-color: initial;
}


/* -------------------------------------------------- 模块 · 显示控制 */
.reveal {
    display: inline;
    font-family: var(--font-secondary);
    position: relative;
    color: var(--reveal-fg);
    cursor: pointer;
    border-radius: 0.25em;
    padding: 0 0.1em;
    transition: all 0.3s ease-in-out;
}

.reveal:not(.active) {
    text-decoration-line: underline;
    text-decoration-style: wavy;
    text-underline-offset: 0.2em;
}

.reveal:not(.active)::after {
    display: inline-block;
    content: "➽";
    text-decoration: none;
}

.reveal:hover {
    color: var(--reveal-fg-hover);
    background-color: var(--reveal-bg-hover);
    text-decoration: none;
}

.secret {
    color: var(--hidden-fg);
    display: none;
}

/* -------------------------------------------------- 模块 · 折叠 Expand */
.caption::before {
  content: "✦";
  margin-right: 0.2em;
  transition: all 0.3s ease-in-out;
  display: inline-block;
}

.caption {
    border: 2px solid var(--caption-border);
    color: var(--caption-fg);
    font-family: var(--font-secondary);
    padding: 0 0.5em;
    border-radius: 0.5em;
    cursor: pointer;
    margin: 0.2em;
    transition: all 0.3s ease-in-out;
    display: inline-block;
}

.caption:hover {
    color: var(--caption-fg-hover);
    background-color: var(--caption-border);
    
}

.caption.active {
    background-color: var(--caption-bg-active);
    border-color: var(--caption-bg-active);
    color: var(--caption-fg-active);
}

.collapse {
    font-family: var(--font-secondary);
    display: none;
}

.expand {
    position: relative;
    margin: 1.2em 0;
}

.expand .expand-surface {
    position: relative;
    display: block;
}

.expand .caption {
    position: relative;
    z-index: 2;
}

.expand .collapse {
    margin-top: 0.6em;
}

.expand.expand-active .expand-surface {
    background-color: rgba(247, 244, 237, 0.92);
    border: 1px solid var(--caption-border);
    border-radius: 0.75em;
    box-shadow: 0 14px 28px rgba(0, 0, 0, 0.12);
    padding: 2.6em 1.2em 1.2em;
}

.expand.expand-active .caption {
    position: absolute;
    top: 0.75em;
    left: 1.0em;
    right: auto;
    margin: 0;
}

.expand.expand-active .collapse {
    display: block;
}

blockquote {
    margin: 1.2em 0;
    padding: 0.8em 1em;
    border-left: 0.35em solid var(--caption-border);
    background-color: var(--option-bg);
    border-radius: 0.4em;
    color: inherit;
}

blockquote p {
    margin: 0;
}

.card.nightMode blockquote {
    border-left-color: var(--caption-border);
    background-color: var(--option-bg);
}



.chaohgg-title-card,
.chaohgg-body-card {
    background-color: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 0.6em;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
    padding: 0.9em 1.6em;
    max-width: var(--card-max-width);
}

.chaohgg-title-card {
    margin: 0.8em 2em 1.1em;
}

.chaohgg-body-card {
    margin: 0.8em 2em 2em;
    display: flex;
    flex-direction: column;
    row-gap: 1.2em;
}

.chaohgg-body-card .chaohgg-field {
    margin: 0;
}

.chaohgg-body-card .chaohgg-tags {
    margin: 1.5em 0 0;
}

.chaohgg-title {
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: clamp(1.8rem, calc(2vw + 1rem), 2.6rem);
    text-align: center;
    margin: 0.15em 0 0.35em;
}

.chaohgg-title h1,
.chaohgg-title h2,
.chaohgg-title h3,
.chaohgg-title p {
    margin: 0;
    font: inherit;
    text-align: inherit;
}

.mobile .chaohgg-title-card,
.mobile .chaohgg-body-card {
    margin: 0.2em;
    margin-bottom: 1em;
    padding: 0.9em 1.25em;
    max-width: 100%;
}

.chaohgg-tags {
    margin: 2em;
    display: flex;
    flex-flow: row wrap;
    justify-content: flex-start;
    gap: 0.5em;
    font-size: 0.8rem;
}

.mobile .chaohgg-tags {
    margin: 1em;
}

.chaohgg-tag {
    all: initial;
    background-color: var(--tag-bg);
    border-radius: 0.25em;
    color: var(--tag-fg);
    display: inline-flex;
    align-items: center;
    font-size: 0.8rem;
    font-family: var(--font-blank);
    padding: 0.25em 0.5em;
    transition: all 0.3s ease-in-out;
    word-break: break-word;
}

.chaohgg-tag:hover {
    background-color: var(--tag-bg-active);
    color: var(--tag-fg-active);
    cursor: pointer;
}

/* -------------------------------------------------- 分隔 */
.chaohgg-divider {
    background-color: transparent;
    border: none;
    border-bottom: 1px dashed var(--divider);
    margin: 1em;
    padding: 0;
}

.chaohgg-divider--answer {
    border-bottom: 1px solid var(--divider);
}

/* -------------------------------------------------- 图片 */
img {
    border-radius: 0.25em;
    display: block;
    margin: 1em auto;
    max-width: var(--img-width) !important;
    transition: max-width 0.25s 0.1s, opacity 0.25s 0.1s, filter 0.1s, transform 0s;
}

.night_mode img {
    filter: var(--img-filter);
    opacity: var(--img-brightness);
}

img:hover {
    cursor: zoom-in;
    filter: none;
    max-width: 100% !important;
    opacity: 1;
}

img + br {
    display: none;
}

html:not(.mobile) img:active {
    border: 1px solid var(--link-fg-active);
    cursor: zoom-out;
    filter: none;
    left: 0;
    max-width: 95% !important;
    opacity: 1;
    position: fixed;
    top: 0;
    transform: translate(calc(50vw - 50%), calc(50vh - 50%));
    z-index: 100;
}

/* -------------------------------------------------- 表格 */
table {
    border-collapse: separate;
    border-spacing: 0;
    width: 100%;
    margin: 1.4em 0;
    border: 1px solid var(--table-border);
    border-radius: 0.65em;
    overflow: hidden;
    color: var(--table-fg);
    background-color: var(--table-bg);
    box-shadow: 0 6px 12px var(--table-shadow);
}

table thead {
    background-color: var(--table-header-bg);
    color: var(--table-header-fg);
    font-weight: 600;
}

table tbody tr:nth-child(odd) {
    background: var(--table-row-odd-bg);
}

table tbody tr:nth-child(even) {
    background: var(--table-row-even-bg);
}

table tr:first-child th:first-child {
    border-top-left-radius: 0.6em;
}

table tr:first-child th:last-child {
    border-top-right-radius: 0.6em;
}

table tr:last-child td:first-child {
    border-bottom-left-radius: 0.6em;
}

table tr:last-child td:last-child {
    border-bottom-right-radius: 0.6em;
}

table th,
table td {
    padding: 0.75em 1em;
    border-left: 1px solid var(--table-border);
    border-bottom: 1px solid var(--table-border);
    text-align: left;
}

table th:last-child,
table td:last-child {
    border-right: 1px solid var(--table-border);
}

table thead tr th {
    border-top: 1px solid var(--table-border);
}

.mobile table {
    font-size: 0.92rem;
}

.mobile table th,
.mobile table td {
    padding: 0.6em;
}

/* -------------------------------------------------- 脚注 */
.footnotes {
    border-top: 1px solid var(--footnote-border);
    margin-top: 1.5em;
    padding: 1em;
    font-size: 0.85rem;
    background-color: var(--footnote-bg);
    border-radius: 0.5em;
    color: var(--footnote-text);
}

.footnotes ol {
    margin-left: 1.5em;
    padding-left: 0.35em;
}

.footnotes li {
    margin-bottom: 0.6em;
}

.footnotes p {
    margin: 0.2em 0;
}

/* MarginNote 链接 */
.mbooks-notelink {
    color: var(--link-fg);
    margin-left: 10px;
    margin-right: 10px;
    -webkit-touch-callout: none;
    -webkit-user-select: none;
    font-size: inherit !important;
    font-family: inherit;
}

.mbooks-linkhighlight {
    color: var(--deck-fg);
    margin-left: 10px;
    margin-right: 10px;
    margin-top: 5px;
    margin-bottom: 5px;
}

.mbooks-linknote {
    color: var(--deck-fg);
    margin-left: 2%;
    margin-right: 5%;
}

.mbooks-booktitle {
    color: var(--caption-fg);
    margin-top: 5px;
    font-size: 70% !important;
}

.mbooks-booktitle0 {
    color: var(--caption-fg);
    margin-top: 5px;
    margin-left: 25px;
    font-size: 70% !important;
}

.card.nightMode .mbooks-linkhighlight,
.card.nightMode .mbooks-linknote {
    color: var(--link-fg);
}

/* -------------------------------------------------- 链接 */
a, a:visited {
    text-decoration: none;
    color: var(--link-fg);
}
a:hover, a:active {
    text-decoration: underline;
    color: var(--link-fg-active);
}

/* -------------------------------------------------- 格式 */
b, strong {
    color: var(--bold-fg);
}

i, em {
    color: var(--italic-fg);
}

b i, b em, strong i, strong em {
    color: var(--bold-italic-fg);
}

u {
    color: var(--underline-fg);
    text-underline-offset: 0.2em;
}

.blank:focus-visible {
    outline: none;
}

@keyframes chaohgg-blank-reveal {
    0% {
        transform: scale(0.9);
        background-color: rgba(255, 255, 255, 0.0);
    }
    60% {
        transform: scale(1.05);
        background-color: rgba(255, 255, 255, 0.3);
    }
    100% {
        transform: scale(1);
        background-color: rgba(255, 255, 255, 0.0);
    }
}
